#include<iostream>

using nmamespace std;

int main
dato = Maria;
izquierda = NULL;
derecha= NULL;
Nodo *n1=new Nodo ("Maria"); //Creamos un nuevo nodo
Nodo *n2=new Nodo ("Juana"); //Creamos un nuevo nodo


Nodo *x=new Nodo(nullptr,"Maria",nullptr);

    Dato =v;
        this-> izq =izq;
        this-> der =der;

        /--------------------------------------------------------------------

        //Juan debe estar arriba, orden de sintaxis.

        Nodo *car=new Nodo(NULL, "carlos",NULL);
                
        Nodo *ine =new Nodo(NULL ,"ines", NULL);

        Nodo *jua=new Nodo(ine,"Juana",carl)

        Nodo* mar =new Nodo(juan,"Maria",jose); //nombres de los primeros letras de su nombre


        Estudiante *es = new Estudiante ("carlo",new Estudiante("pedro",NULL));


//////////////////////////////////////////////////////////////////
ar->BuscarNodo(5);
ar->Elminar(23);
si existe lo puedes eleminar si no , no puedes elminarlo
condicion si es nuull existe si no ezite 

//ESta es la recursion
//tiene que retorna la direccion de memoris, por que tiene que elminarlo
Nodo<T> *BuscarUnNodo ( T v){
    //Tengo que pasarle donde va comenzar
    return BuscarNodo(raiz,v);
}

Nodo<T> *BuscarUnNodo (<T> *r,T v){

    // r es el 7 , luego tan , leugo tan
    // v es lo que estamos buscando
    if(r->retDato()==v){
        //debe retornar el r
        retDato r;
    }
    //cual es mayo, para donde se ira, izauierdo derecjo
    else if (r->retDato > v)
        // se ira para la izquierda
        //tiene que ser return 
        return BuscarNodo(r->retHijoIzq(),v);
    else if(r->retDato<v){
        return BuscarNodo(r->retHijoDer(),v);
    }
}

n->asigPadre(r);

int main(

    //ESta bucando un nodo
    ar->BuscarNodo(5)->retDato();

    Nodo<int> *n=ar->BuscarNodo(5);
    cout<<n->retpadre()->retDato()<<endl;

    //la reiz tiene padre? hacer ese metodo mas.

    //MIERCOLES UNA PRACTICA, TEMA ARBOLESS.


if(dfsdfsfas){
    r->unirConIzq(n);
    n->asigPadre(r);
    sfdsfsafsd;
    adfsadfsaf;
}   
// esto es igual 
if(dsfjasjflds)
    r->unirConDer,n->asigPadre();
